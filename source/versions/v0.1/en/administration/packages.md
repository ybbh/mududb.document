# Application Packages

MuduDB applications are deployed as `.mpk` packages. A package usually contains:

- WebAssembly components
- schema definitions
- initialization data
- procedure metadata

Use `mpk` to build packages and `mcli app-install` to install them. See {doc}`../tutorials/wallet` for a complete example.
