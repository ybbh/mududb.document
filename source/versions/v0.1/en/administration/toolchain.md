# Toolchain

The MuduDB toolchain is organized around procedure applications:

- `mgen`: generate types, interfaces, and procedure-related code.
- `mtp`: transform procedure source code so synchronous-style code can compile into asynchronous execution form.
- `mpk`: package WebAssembly, schema, initialization data, and metadata.

