# WebAssembly Integration

MuduDB uses the WebAssembly Component Model to host procedure modules. Related documentation should cover component interfaces, host imports, package format, and runtime limits.
