# Kernel

The kernel provides the database correctness foundation, including storage, transactions, scheduling, and query execution. Internal documentation should describe subsystem boundaries without turning implementation details into user-facing compatibility promises.
