# v0.1

`v0.1` is the initial documentation version directory for organizing existing installation, command, procedure application, and architecture material.

Future release notes should record:

- new features
- behavior changes
- compatibility impact
- migration steps
- known limitations
