# 快速开始

本章面向第一次安装和运行 MuduDB 的用户。

```{toctree}
:maxdepth: 2

install
first-session
```

## 安装路径

MuduDB 提供两种常见路径：

````{grid} 1 1 2 2
```{grid-item-card} 发行版安装
适合服务器部署、日常试用和验证功能。优先使用 `mudup install` 安装发布产物。
```

```{grid-item-card} 源码构建
适合开发者修改数据库内核、工具链、API 绑定或示例应用。
```
````
