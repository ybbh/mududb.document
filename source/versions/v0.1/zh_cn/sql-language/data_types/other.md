# 其他类型

## ARRAY

一个有序的、可变长度的元素集合，元素共享相同的类型。数组语法和操作在存储层受支持；SQL 数组构造器（例如，`ARRAY[1, 2, 3]`）在 v0.1 中尚不可用。

## RECORD

一个具有命名字段的结构化对象，每个字段都有自己的类型。记录用于嵌套数据以及过程的复杂返回值。

## BINARY

原始字节序列。适用于存储不透明载荷、序列化对象或二进制文件内容。
