# SQL 语言

MuduDB 使用一种 SQL 方言，该方言由专为其存储和执行模型设计的 tree-sitter 语法解析。该方言涵盖用于模式定义的 DDL、用于数据变更的 DML，以及用于单表查询的 SELECT。本节描述每个类别、MuduDB 接受的语法，以及当前支持的限制范围。

```{toctree}
:maxdepth: 2

ddl/index
dml/index
query
data_types/index
```
